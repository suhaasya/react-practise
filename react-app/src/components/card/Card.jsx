import React from "react";

const Card = ({ children, count }) => {
  console.log(props.children);
  console.log(props.count);

  return <div></div>;
};

export const Card2 = ({ children, count }) => {
  console.log(props.children);
  console.log(props.count);

  return <div></div>;
};

export default Card;

export const num = 10;
